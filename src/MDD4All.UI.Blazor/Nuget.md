A breadcrumb trail and a set of toolbar buttons for Blazor.

The breadcrumb shows the path to whatever is selected and lets any step of it be clicked.
The toolbar button brings its own icons - create, add, delete, delete mode, move up and
down - and one appearance, so the same action looks the same wherever it appears.

Small building blocks. This is not a component framework and does not try to be one.
